name: ARC Kubernetes Validation

on:
  workflow_dispatch:

jobs:
  arc-validation:
    runs-on: self-hosted

    steps:
      - name: Show Hostname
        run: hostname

      - name: Check Kubectl
        run: |
          which kubectl || true
          kubectl version --client || true
